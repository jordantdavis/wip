## 1. Implementation

- [x] 1.1 Add `version` entry to `printUsage()` in `main.go`

## 2. Verification

- [x] 2.1 Run `wip` with no arguments and confirm `version` appears in output
- [x] 2.2 Run `go vet ./...` to confirm no issues
